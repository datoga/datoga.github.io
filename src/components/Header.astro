---
const navLinks = [
    { href: "/", label: "Home" },
    { href: "/about/", label: "About" },
    { href: "https://github.com/datoga", label: "GitHub", external: true },
];
---

<header
    class="sticky top-0 z-50 backdrop-blur-lg bg-slate-900/80 border-b border-slate-800/50"
>
    <nav class="max-w-4xl mx-auto px-6 py-4 flex items-center justify-between">
        <a href="/" class="hover:opacity-80 transition-opacity">
            <img src="/logo.svg" alt="David Torres" class="h-10 w-10" />
        </a>
        <ul class="flex gap-6">
            {
                navLinks.map((link) => (
                    <li>
                        <a
                            href={link.href}
                            class="text-slate-400 hover:text-cyan-400 transition-colors font-medium"
                            target={link.external ? "_blank" : undefined}
                            rel={
                                link.external
                                    ? "noopener noreferrer"
                                    : undefined
                            }
                        >
                            {link.label}
                        </a>
                    </li>
                ))
            }
        </ul>
    </nav>
</header>
